"""The harness: the deterministic engineering around the model.

Per the whitepaper, ~98% of an agent system is this operational infrastructure
(tool dispatch, approval gating, context budgeting, audit) and only ~2% is the
model call itself. That ratio is the whole point of this package.
"""
