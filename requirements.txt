# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sse-starlette==2.2.1

# --- RAG / vector DB ---
qdrant-client==1.12.2
fastembed==0.5.1          # local ONNX embeddings, no torch, runs fully offline

# --- Agent protocols & LLMs ---
mcp==1.2.0                # official Model Context Protocol SDK (server + client)
anthropic==0.42.0         # default LLM provider (Claude)
httpx==0.28.1             # used by the Ollama client and ingestion

# --- Dev / tests ---
pytest==8.3.4
pytest-asyncio==0.25.2
